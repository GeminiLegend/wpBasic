<?php
// 
// $name = 'Name'; variable
// echo $name; 
// var_dump($name);
// die("");
// arrays:print_r
// custom debbuger kint
// hw 
// seperate code that should be on every page into a header and a footer
// have wp load that 
// *not a require or include or enqueue
// what files need to be included in the theme
// what is the wp loop and how does it work
//
// an index.php with a post loop and a style.css
// with a theme name description author and version
// are the bare minimum needed to be includedf/while loop that checks if there are
// any posts and if theyre are it will repeat untill
// the have_posts() function is false.
// the_post() takes the current item and makes it
// available inside the current itteration of The Loop
// confused on how to use the_post
// the_content() fetches the content of the post
// filters it and displays it  
// <?php endwhile;?'>' ends the loop
// after^ posts_nav_link can be used to add other pages
// <?php posts_nav_link( $sep, $prelabel, $nextlabel ); ?'>'
// $sep text $prelabel link text to prev page
// $nxtlabel link text to next page
// get_next/previous_posts_link():
// post queries are in reverse chronological order
// next -older prev -newer
// <?php echo get_next_link($label, $max_page); ?'>'
// $label the content for the link text
// $max_page limit number of pages displayed 0=none

